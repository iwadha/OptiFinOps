<section id="how-it-works" class="py-20 bg-gray-50 dark:bg-gray-800">
  <div class="container mx-auto px-6">
    <div class="max-w-4xl mx-auto text-center mb-12">
      <h2 class="text-4xl font-bold mb-6">How It Works</h2>
      <p class="text-xl mb-8">
        By automating the heavy lifting of FinOps, focus on innovation, not cost management.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-8">
      {[
        {
          title: "Comprehensive Assessment",
          icon: "fas fa-search",
          description: "We analyze your current cloud usage, pricing models, and cost allocation to uncover inefficiencies and untapped savings opportunities."
        },
        {
          title: "Intelligent Automation",
          icon: "fas fa-robot",
          description: "Our automated engine dynamically optimizes your cloud commitments in real-time, including continuous cost rebalancing and automated scaling."
        },
        {
          title: "Real-Time Monitoring",
          icon: "fas fa-chart-line",
          description: "Get transparent, real-time reporting with complete visibility into your cloud spend, savings achieved, and utilization metrics."
        }
      ].map(step => (
        <div class="bg-white dark:bg-gray-900 rounded-lg p-6 shadow-lg">
          <div class="text-4xl text-blue-600 mb-4">
            <i class={step.icon}></i>
          </div>
          <h3 class="text-xl font-bold mb-4">{step.title}</h3>
          <p class="text-gray-600 dark:text-gray-400">{step.description}</p>
        </div>
      ))}
    </div>

    <div class="flex justify-center gap-8 mt-8">
      {[
        {
          title: "Strategic Guidance",
          icon: "fas fa-compass",
          description: "Our FinOps experts collaborate with your stakeholders to align cloud financial management with your business priorities."
        },
        {
          title: "Continuous Optimization",
          icon: "fas fa-sync",
          description: "Our system works tirelessly to ensure your cost optimization adapts dynamically to changes in usage, workloads, or provider pricing."
        }
      ].map(step => (
        <div class="bg-white dark:bg-gray-900 rounded-lg p-6 shadow-lg md:w-1/3">
          <div class="text-4xl text-blue-600 mb-4">
            <i class={step.icon}></i>
          </div>
          <h3 class="text-xl font-bold mb-4">{step.title}</h3>
          <p class="text-gray-600 dark:text-gray-400">{step.description}</p>
        </div>
      ))}
    </div>

    <div class="text-center mt-12">
      <button
        class="bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 transition-colors duration-200"
        onclick="document.getElementById('contactModal').classList.remove('hidden')"
      >
        Let's Talk
      </button>
    </div>
  </div>
</section>